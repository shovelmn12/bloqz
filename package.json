{
  "name": "bloqz",
  "private": true,
  "packageManager": "pnpm@12.6.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint:pkg": "pnpm -r exec publint --strict && pnpm -r exec attw --pack . --profile esm-only",
    "changeset": "changeset",
    "publish": "pnpm -r publish"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.5",
    "@changesets/cli": "^3.0.3",
    "@types/node": "^24.10.1",
    "publint": "^0.3.24",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
