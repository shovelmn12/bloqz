# Changelog

All notable changes to this project will be documented in this file.
This project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
Entries from this release on are generated by [Changesets](https://github.com/changesets/changesets);
older entries follow [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## 3.0.0

### Major Changes

- 5815c08: **Breaking:** `Relay` has a new required `readonly isDisposed: boolean`. Hand-written `Relay` implementations (mocks, stubs) must add it.

  - After `dispose()`, `emit` and `on` log a `console.warn` and do nothing, and `on` returns a no-op unsubscribe. `dispose()` stays idempotent.
  - `on('*', (topic, event) => ...)` now resolves to the wildcard overload. An event-only handler can no longer be registered on `'*'`.
  - A subscriber that throws is isolated: other subscribers still receive the event and `emit` never throws. The error goes to the new `createRelay({ onError })` option, or to `console.error` by default.
  - Event maps can be plain interfaces: `Relay` and `createRelay` are constrained by the new `RelayEventsMapOf<Events>`. `RelayEventsMap` stays the default. `RelayTopicHandler` and `RelayEventsMapOf` are exported.
  - The unused `RelayPredicate` type is deprecated.
  - Dropped the unused `@bloqz/core` peer dependency.

### Patch Changes

- 5815c08: Packaging fixes:

  - Published source maps embed their sources (`inlineSources`) instead of pointing at unpublished `src/` paths. `@bloqz/core` ships its `.map` files again.
  - Exports maps are valid and consistent: a `"."` entry with `types` → `import` → `default`, plus `./package.json`. `main`, `module` and `types` are set, and so is `sideEffects: false`.
  - Tarballs contain only `dist`, `README.md`, `CHANGELOG.md` and `package.json`.

## [2.0.3] - 2026-08-13

### Changed
- Aligned the test suite with the monorepo conventions (moved tests to `tests/`, unified quoting and import style).

## [2.0.2] - 2026-02-15

### Changed
- Updated README.md to accurately reflect the current implementation (removed outdated pattern matching and predicate examples).
- Added comprehensive documentation and examples for TypeScript generics and type safety in README.md.

## [2.0.1] - 2026-02-13

### Added
- Exported `RelayEventsMap` type from the main entry point.
- Improved `createRelay` type constraint to use `RelayEventsMap`.

### Changed
- Improved code formatting in `createRelay`.

## [2.0.0] - 2026-02-13

### Changed
- **Relay Interface:** Simplified the `Relay` interface to focus on exact topic subscriptions.
- **Removed:** Support for topic patterns (regex) and predicate-based subscriptions in `on`.
- **API Change:** `RelayHandler` now only receives the event payload for specific topic subscriptions.
- **API Change:** Added explicit support for the `*` wildcard topic, which receives both `topic` and `event`.

## [1.2.0] - 2025-12-03

### Changed
- Synchronize all packages to version 1.2.0.

## [1.1.4] - 2025-12-03

### Changed
- RelayEvent supports extra params.

## [1.1.3] - 2025-12-03

### Changed
- Synchronize all packages to version 1.1.3.

## [1.1.2] - 2025-12-03

### Changed
- Synchronize all packages to version 1.1.2.

## [1.0.4] - 2025-11-18

### Changed
- **Exports:** Refactored the main entry point (`index.ts`) to use explicit, named exports instead of wildcards. This improves tree-shaking for consumers.

## [1.0.3] - 2025-10-23

### Changed
- Aligned all packages to version 1.0.3.

## [1.0.2] - 2025-10-22

### Added
- Added tests for the `createRelay` function.
